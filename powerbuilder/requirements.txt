# Web Framework
django>=5.0.0
django-environ==0.11.2  # For managing .env variables
django-htmx>=0.3.0     # For HTMX integration in Django templates

# Production WSGI server (used on Render preview + droplet prod)
gunicorn>=21.2.0       # WSGI HTTP server for prod
whitenoise>=6.6.0      # Serve static files from Django process (no S3 needed for preview)

# AI Orchestration & State
langgraph>=0.2.0        # The Orchestrator engine
langchain>=0.3.0        # Foundational AI components
langchain-openai>=0.2.0 # OpenAI specific integration
langchain-community>=0.3.0 # Extra community tools (like PDF loaders)

# Vector Database
pinecone-client>=4.1.0  # Pinecone Python SDK
langchain-pinecone>=0.2.0 # Bridge between LangChain and Pinecone

# Document Parsing & Export
llama-parse>=0.5.0      # For high-fidelity PDF/table extraction
python-docx==1.1.0      # For creating Word documents programmatically
openpyxl>=3.1.0         # For creating Excel workbooks (.xlsx)
markdown==3.5.2         # To parse AI Markdown output
beautifulsoup4==4.12.3  # Required for some document conversions

# Utilities
python-dotenv==1.0.1    # To load your .env file
openai>=1.12.0          # The core OpenAI library
pydantic>=2.6.0         # For data validation in state.py
pandas>=2.0.0           # For election data and precinct CSV processing
requests>=2.31.0        # For Census and FEC API calls

# Geospatial
geopandas>=0.14.0       # Spatial joins and TIGER/Line shapefile loading
topojson>=1.5           # TopoJSON parsing fallback if fiona driver unavailable